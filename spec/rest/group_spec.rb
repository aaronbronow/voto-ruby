require 'spec_helper'

describe VotoMobile::REST::Group do
  before do
    # @client = VotoMobile::REST::Client.new('someToken')
    # @group = VotoMobile::REST::Group.new('someName', @client)
  end

  xit 'gets a list of groups' do
    # response, url = stub_get(:groups, 'Groups')
    # expect(VotoMobile::REST::Group.list).to eq(response)
  end

  it 'gets a specific group'
  it 'is created'
end